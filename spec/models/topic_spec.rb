require 'rails_helper'

RSpec.describe Topic, type: :model do
	context "is valid" do 
		it "with a user"
		it "with a valid title"
		it "with a title that is unique for the same user"
		it "with a title that is not unique for different users"
	end
end